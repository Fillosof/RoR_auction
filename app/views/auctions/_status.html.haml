%p
  -if @product.auction.top_bid.user_id == current_user.id
    Ви виграли цей лот зі ставкою
  -else
    Ви програли цей лот, максмальна ставка була:
  %b
    =@product.auction.top_bid.value
    Грн.
  -if @product.auction.top_bid.user_id == current_user.id
    %p
      =link_to "Отримати товар", transfer_product_path(@product), class: "btn btn-primary", method: :put
